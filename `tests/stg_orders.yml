version: 2

models:
  - name: stg_orders
    tests:
      - unique: order_id
      - not_null: [order_id, customer_id]
      - accepted_values:
          column_name: order_status
          values: ['created', 'approved', 'shipped', 'delivered']
